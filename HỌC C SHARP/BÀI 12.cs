// CÁC HÀM TOÁN HỌC THƯỜNG DÙNG

/*
using System.Text;

namespace HỌC_C_SHARP
{
    internal class BÀI_12
    {
        public static void Main()
        {
            bai12();
        }
        public static void bai12()
        {
            Console.OutputEncoding = Encoding.UTF8;
            Console.WriteLine("Pi="+Math.PI);
            Console.WriteLine("Kiểu dữ liệu của math.pi = " +Math.PI.GetType().ToString());
            
            // căn bậc 2
            int a = 4;
            int b = 10;
            Console.WriteLine("Căn bậc 2 của a là " + Math.Sqrt(a));
            Console.WriteLine("Kiểu dữ liệu của math.Sqrt(a) = " + Math.Sqrt(a).GetType().ToString());

            // lũy thừa
            Console.WriteLine("2 lũy thừa 3 = "+Math.Pow(2,3));

            // min
            Console.WriteLine("min = "+Math.Min(a,b));

            // làm tròn
            float n = 1.23456789f;
            Console.WriteLine("Số n sau khi làm tròn 2 chữ số là "+Math.Round(n,2));

            // sin
            // phải đổi sang radian, nhân số độ với pi/180
            Console.WriteLine("sin90 = "+Math.Sin(90*Math.PI/180));

            Console.ReadLine();
        }
    }
}
*/